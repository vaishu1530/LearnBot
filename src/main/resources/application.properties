spring.application.name=learnbot

# --- DATABASE ---
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/learnbot_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:vaishu}

# --- GEMINI AI (DECEMBER 2025 STABLE FIX) ---
# Use gemini-3-flash-preview for PhD-level reasoning and fast diagrams
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-3-flash-preview:generateContent
# The ":" prevents the application from crashing if the variable is missing
gemini.api.key=${GEMINI_API_KEY:}
# --- OTHER ---
spring.jpa.hibernate.ddl-auto=update
spring.thymeleaf.cache=false